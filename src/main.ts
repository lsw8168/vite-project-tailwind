export const sum = (x: number, y: number): number => x + y;

export function greetingsES5(name: string) {
  return 'hello ' + name;
}

export const greetingsES6 = (name: string) => {
  return `hello ${name}`;
};
